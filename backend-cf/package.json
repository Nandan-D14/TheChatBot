{
  "name": "thechatbot-backend-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "typecheck": "tsc --noEmit",
    "db:init:local": "wrangler d1 execute DB --local --file migrations/001_init.sql",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.2",
    "@cloudflare/workers-types": "^4.20250303.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.5.0"
  }
}
